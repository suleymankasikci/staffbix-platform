/**
 * Stage B placeholder — Approval Center section lands here, between Hero
 * and Problem. Per design direction: the section right after Hero
 * communicates "the AI literally cannot go rogue" — push notification
 * mockup, hard caps, audit log.
 *
 * This is a quiet marker so the Stage A homepage shows where Stage B
 * will drop in. Delete this file in Stage B and replace its usage in
 * app/page.tsx with the real <ApprovalCenter />.
 */
export function StageBSlot() {
  return null;
}
